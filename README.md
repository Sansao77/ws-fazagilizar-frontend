# Projeto WS-FazAgilizar (Front)

- Esse projeto os frameworks Ionic + React
- Para iniciar o projeto é utilizado o comando:

    ```bash
    npm run dev
    ```

    - Obs: Ele será inicializado pelo Vite automaticamente na porta 5173